<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * (S22) مجموعة حالات الحضور الجديدة بطلب صريح من يحيى: حاضر/مستأذن/غائب
 * بعذر/غائب بدون عذر — تحلّ محلّ (حاضر/غائب/متأخر/مستأذن) القديمة. لا تعديل
 * على بنية الجدول (status عمود نصّي عام أصلًا منذ S9)، فقط ترحيل بيانات
 * السجلّات القديمة إلى القيم الجديدة، بقرارين صادرين عن يحيى صراحةً لا
 * افتراضًا:
 *
 *  - كل سجلّ "متأخر" قديم → "حاضر" (الطالب حضر فعليًا، مجرّد تأخّر لم يعد
 *    حالة مستقلّة).
 *  - كل سجلّ "غائب" قديم (بلا تمييز عذر أصلًا) → "غائب بدون عذر" (الأقرب
 *    لدلالة "غائب" المجرّدة القديمة قبل إضافة تمييز العذر).
 *
 * down() لا يمكنها استرجاع الحالة الأصلية بدقّة (لا فرق مخزَّن بين "متأخر"
 * الذي صار "حاضر" وحضور حقيقي أصلي، ولا بين "غائب بدون عذر" المُرحَّل و"غائب
 * بدون عذر" مُسجَّل حديثًا فعلًا) — قرار مقصود موثَّق هنا بدل down() صامتة
 * توهم بإمكان تراجع كامل غير موجود فعليًا.
 */
return new class extends Migration
{
    public function up()
    {
        DB::table('attendances')->where('status', 'متأخر')->update(['status' => 'حاضر']);
        DB::table('attendances')->where('status', 'غائب')->update(['status' => 'غائب بدون عذر']);
    }

    public function down()
    {
        // غير قابلة للعكس بدقّة — راجع تعليق الصنف أعلاه.
    }
};

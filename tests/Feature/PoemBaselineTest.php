<?php

namespace Tests\Feature;

use Tests\TestCase;

/**
 * (S24 — بطلب صريح من يحيى): "أرضية متن" اليدوية (كانت هذا الملف يختبرها،
 * S16) أُلغيت نهائيًا — نفس القرار وبنفس السبب الذي أُلغيت به أرضية حفظ
 * القرآن اليدوية سابقًا (راجع MemorizationProgress::applyCascade()). الاعتماد
 * الآن كليًا على مديات from_bayt..to_bayt الفعلية المسجَّلة (راجع
 * PoemProgress). الجدول (student_poem_baselines) أُسقط بهجرة جديدة
 * (2026_09_21_000001)، والمسار/النموذج/المتحكّم لم تعد مستعمَلة.
 *
 * لم يُحذف هذا الملف فعليًا (قيد تقني في أداة الربط بجهازك لا يسمح بحذف
 * ملفات — فقط بكتابتها) — تُرك بلا أي اختبار عمدًا. يمكنك حذفه يدويًا إن
 * أردت: tests/Feature/PoemBaselineTest.php.
 */
class PoemBaselineTest extends TestCase
{
    // لا اختبارات — الميزة المختبَرة هنا أُلغيت نهائيًا.
}
